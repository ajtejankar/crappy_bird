[project]
name = "crappy-bird"
version = "0.2.0"
description = "A deliberately awful browser game that is also a live RL environment for a coding agent. No money, ever."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]

[tool.uv]
package = false
