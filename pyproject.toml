[project]
name = "crappy-bird"
version = "0.1.0"
description = "A game that funds its own AI-driven enshittification, one dollar at a time."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "stripe>=10.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
